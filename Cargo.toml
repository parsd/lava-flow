[package]
name = "lava-flow"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }
hostname = "0.4"

[dev-dependencies]
serde_json = "1.0"
